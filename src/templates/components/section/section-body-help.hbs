<main class="section-body section-body--help">
  <div class="splide">
    <div class="splide__track">
      <ul class="splide__list">
        {{#each data.treatments}}
        <li class="splide__slide">
          {{> ../card/card-help data=this }}
        </li>
        {{/each}}
      </ul>
    </div>
  </div>
</main>

{{!-- <main class="section-body section-body--help">
  <div class="carrossel">
    {{#each data.treatments}}
    {{> ../card/card-help data=this }}
    {{/each}}
  </div>
</main> --}}